实验1：排序 n 个元素，元素为随机生成的长为1..16的字符串，n的取值为：
        2^3，2^6，2^9，2^12，2^15，2^18，2^21；
     算法：直接插入排序，堆排序，归并排序，快速排序。
实验2：排序n个元素，元素为随机生成的1..65535的正整数，n的取值为：
        2^3，2^6，2^9，2^12，2^15，2^18，2^21
    算法：直接插入排序，快速排序，归并排序，
       基数排序，计数排序。
